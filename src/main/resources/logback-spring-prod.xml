<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="app.name" source="spring.application.name" defaultValue="mapsbridge"/>
    <springProperty scope="context" name="app.version" source="app.version" defaultValue="1.0.0"/>
    <springProperty scope="context" name="app.environment" source="spring.profiles.active" defaultValue="production"/>
    <springProperty scope="context" name="cloudwatch.log.group" source="cloudwatch.logging.log-group-name"
                    defaultValue="mapsbridge-log-group"/>
    <springProperty scope="context" name="cloudwatch.log.stream" source="cloudwatch.logging.log-stream-name"
                    defaultValue="mapsbridge-stream"/>
    <springProperty scope="context" name="cloudwatch.region" source="cloudwatch.logging.region"
                    defaultValue="eu-north-1"/>

    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- CloudWatch appender with Logstash JSON encoder -->
    <appender name="CLOUDWATCH" class="com.example.mapsbridge.config.logging.cloudwatch.CloudWatchLogAppender">
        <logGroupName>${cloudwatch.log.group}</logGroupName>
        <logStreamName>${cloudwatch.log.stream}</logStreamName>
        <region>${cloudwatch.region}</region>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"application":"${app.name}","environment":"${app.environment}","version":"${app.version}"}
            </customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <level>level</level>
                <logger>logger</logger>
                <thread>thread</thread>
                <message>message</message>
            </fieldNames>
            <includeContext>true</includeContext>
            <includeMdc>true</includeMdc>
        </encoder>
    </appender>

    <!-- Async wrapper for CloudWatch to improve performance -->
    <appender name="ASYNC_CLOUDWATCH" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CLOUDWATCH"/>
        <queueSize>1000</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>true</includeCallerData>
        <includeMdc>true</includeMdc>
    </appender>

    <!-- Production-specific loggers - More restrictive -->
    <logger name="com.example.mapsbridge" level="INFO"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="software.amazon.awssdk" level="WARN"/>
    <logger name="org.apache.http" level="WARN"/>
    <logger name="org.springframework.security" level="WARN"/>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_CLOUDWATCH"/>
    </root>
</configuration>